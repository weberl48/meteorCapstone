Accounts.ui.config({
  passwordSignupFields: 'USERNAME_ONLY'
});
// tell our accounts system that we want users to log-in via a username, we simply add an Accounts.ui config block in a new config.js file inside client/helpers/:
